// Repository-authored editorial content; no visitor HTML is accepted.
export default "A small HTML file is useful only if it still renders the intended words and structure. Removing every space between tags can join words, while deleting comments can interfere with a framework that uses them as markers. A conservative minification workflow begins by deciding what must remain untouched.\n\nThe [HTML Minifier](/html-minifier) reduces whitespace within ordinary tag syntax and removes ordinary comments. It deliberately preserves text-node spacing and raw or preformatted sections. It also performs a parser-based syntax-feedback pass before applying its conservative transformation to the original source.\n\n## Try a small static example\n\nUse a div with several spaces between its attributes and an ordinary explanatory comment. The output can reduce the tag's extra attribute spacing and remove that comment while keeping the text inside the div unchanged.\n\nFor a phrase split across inline elements, preserving the text spaces matters more than achieving the largest character reduction. Compare the rendered words before and after rather than judging the result only by the completion message.\n\nThe interface reports input and output character counts. A small reduction can be the correct outcome for a source dominated by meaningful text. It is not a compression benchmark or a promise of a particular loading-time improvement.\n\n## What the conservative routine preserves\n\nThe current implementation preserves sections such as script, style, pre and textarea, along with other listed raw-text handling. It does not minify embedded JavaScript or CSS. It also retains conditional and recognised special comments rather than treating every comment as disposable.\n\nThat policy reduces some common risks but is not a universal framework-compatibility guarantee. A template can rely on an ordinary-looking comment or syntax the routine does not understand. Test the actual framework output before using the result in a live application.\n\nOptional tags, attribute semantics and sanitisation are outside its goals. It is not a tool for stripping dangerous HTML or proving that an input is safe to render.\n\n## Use the worker-based interface\n\n1. Preserve the original static HTML source.\n2. Paste between 1 and 250,000 characters into Source code.\n3. Choose Minify HTML and wait for completion or syntax feedback.\n4. Compare text, comments with functional significance and attribute values.\n5. Copy the output or download the generated result.\n6. Test the result in the page and build system that will use it.\n\nProcessing runs in a separate browser worker with a 30-second timeout. If it times out or cannot load the processor, try a smaller representative input after checking the error. Repeatedly submitting a huge document does not improve confidence in the result.\n\nEditing the source clears the prior output in this shared interface, which helps avoid copying a result for an earlier input. Still check the final downloaded file before replacing an existing asset.\n\n## Review behaviour, not just source size\n\nTest visible text spacing around inline elements, preformatted examples, form values and any template markers. Check relevant desktop and mobile views. If a component uses hydration or another client-side reconciliation process, inspect the browser console and interactive behaviour too.\n\nThe parser feedback catches some malformed syntax; it does not test links, accessibility, external resources or application logic. A page can parse and still be the wrong page for the task.\n\n[HTML Formatter](/html-formatter) is a separate lightweight readability tool with its own heuristic limitations. [CSS Formatter](/css-formatter) provides parser-based formatting for plain CSS. Choose the operation that matches the source rather than sending every language through one minifier.\n\n## Source handling and privacy\n\nThe source is processed in the browser worker rather than uploaded to a minification service. It is not executed by this tool. However, embedded private URLs, tokens and customer text remain sensitive in both source and output.\n\nUse an appropriate development environment for confidential projects and consult the [Privacy Policy](/privacy-policy) for site-level context. For static markup that fits the supported limits, the [HTML Minifier](/html-minifier) offers a conservative output to review—not a reason to skip the page's normal tests.\n\n";
