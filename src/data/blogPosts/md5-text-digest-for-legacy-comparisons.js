// Repository-authored editorial content; no visitor HTML is accepted.
export default "Some older systems still request an MD5 value for a text comparison or compatibility check. Producing that value correctly is a different question from deciding whether MD5 is suitable for a security design.\n\nThe [MD5 Hash tool](/md5-hash) now computes a genuine MD5 digest using the installed hash implementation. It encodes the exact input as UTF-8 and returns lowercase hexadecimal. It is not a demonstration hash, a file-upload checker or a password-storage system.\n\n## Verify exact text with a known value\n\nThe text abc, with no added newline, produces 900150983cd24fb0d6963f7d28e17f72. The output has 32 hexadecimal characters. Adding a space or changing a capital changes the bytes and therefore the digest.\n\nEmpty input is supported and produces a digest too. A blank string is not the same input as a single space. When comparing systems, record whether the source includes a trailing newline, which text encoding is used and whether any normalisation happens before hashing.\n\n## Use a careful comparison workflow\n\n1. Establish that the legacy consumer specifically requires MD5.\n2. Copy the exact nonsensitive text, preserving intended whitespace.\n3. Generate the digest and compare all characters with the expected value.\n4. If it differs, inspect input bytes and encoding before changing algorithms.\n5. Keep the original input definition with the comparison result.\n\nThe input limit is 1,000,000 JavaScript string characters. This interface hashes text, not raw bytes from an uploaded file. Copying a file's visible text can change line endings or omit binary data, so it is not a reliable substitute for hashing the file itself.\n\n## Do not use MD5 as a trust guarantee\n\nMD5 has known collision weaknesses. A matching MD5 value is not a sound basis for collision-resistant security checks, digital signatures or password storage. For an adversarial integrity problem, select a current appropriate construction and a trusted source of the expected value.\n\n[SHA-256 Hash](/sha256-hash) computes another text digest, but a plain hash still does not authenticate a sender. [HMAC Generator](/hmac-generator) introduces a secret-key construction for a different task. Neither should be described as encrypting the message.\n\n## Local hashing still needs careful handling\n\nThe operation runs in the browser without uploading the text to a hash server. A digest does not necessarily hide a predictable input: someone can hash guesses and compare them. Do not publish a digest of a sensitive low-entropy value as though it were anonymised data.\n\nThe [Privacy Policy](/privacy-policy) covers site context. Use the [MD5 tool](/md5-hash) for a clearly bounded legacy comparison, and keep security-design decisions separate from the successful computation of a historical algorithm.\n\n";
