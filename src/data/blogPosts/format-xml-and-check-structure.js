// Repository-authored editorial content; no visitor HTML is accepted.
export default "An XML document can be difficult to inspect when every element appears on one line. Indentation helps reveal nesting, but it does not tell you whether a partner's schema accepts the document or whether whitespace inside mixed content carries meaning.\n\nThe [XML Formatter](/xml-formatter) parses input with the browser's XML parser, checks for a parse error, serialises the document and adds indentation using simple tag-oriented rules. It is a useful inspection aid for small structured examples. It is not an XSD validator, canonicalisation tool or guarantee of byte-for-byte preservation.\n\n## Begin with well-formed structure\n\nTry `<order><id>17</id><status>open</status></order>`. The outer order element contains two child elements. Formatting makes those boundaries easier to scan without requiring you to count adjacent closing tags manually.\n\nNow remove the closing status tag. The parser should report malformed XML rather than simply indenting the broken source. Check matched tag names, quoted attribute values and a single appropriate document root when investigating an error.\n\nWell-formedness is only the first layer. A receiving system may require another element, a namespace or a constrained value even when the document parses successfully. Those requirements belong to its schema or application contract.\n\n## Use a source-preserving review workflow\n\n1. Retain the original file or response.\n2. Paste a small representative document and choose Format.\n3. Resolve any XML parse error without deleting fields merely to make the message disappear.\n4. Compare element names, values, attributes and namespaces with the source.\n5. Validate the reviewed document with the receiving system's actual schema where required.\n6. Copy the formatted text only when a reserialised representation is appropriate.\n\nThe parser and serializer may change presentation details before the indentation routine runs. Do not use this output to compare a digital signature or reproduce an exact original byte sequence. A readable representation and a canonical signed representation are different artifacts.\n\n## Mixed content needs special attention\n\nAn element can contain text before and after child elements. In a phrase such as `<p>Hello <b>reader</b> again</p>`, spaces around the child element affect the resulting text. Treat a formatter as potentially significant to such content rather than assuming every inserted or removed whitespace character is decorative.\n\nThe indentation phase uses textual patterns after serialization. It is not a full semantic pretty-printer for every XML vocabulary. Preformatted text, mixed content and application-specific whitespace rules should be reviewed with tooling designed for that format.\n\nFor data-oriented records with simple leaf values, indentation is often helpful. For publishing formats, signed documents or specialised interchange, preserve the original and use the authoritative workflow.\n\n## Formatting and conversion are different decisions\n\n[XML to JSON](/xml-to-json) changes the representation and has its own attribute and mixed-content limitations. [JSON to XML](/json-to-xml) creates elements according to another mapping. They do not form a universal reversible pair.\n\nIf your only problem is readability, do not convert formats unnecessarily. Keep XML as XML, inspect the hierarchy and test it with its actual consumer. Conversion can introduce type and structural questions that indentation alone would not create.\n\n## Handle source data deliberately\n\nThe formatting operation takes place in the browser, without sending the XML to a formatting service. That does not make customer records or embedded credentials suitable for a shared screenshot. Redact a sample while preserving the structure needed to reproduce the error.\n\nLarge documents can consume substantial browser memory, and this interface is not a streaming validator. The [Privacy Policy](/privacy-policy) describes the site context. Use the [XML Formatter](/xml-formatter) for a readable inspection copy and retain the original for authoritative processing.\n\n";
