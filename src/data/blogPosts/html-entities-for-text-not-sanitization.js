// Repository-authored editorial content; no visitor HTML is accepted.
export default "You want a tutorial to show a tag literally, but the browser interprets it as markup. HTML entities provide a textual representation of characters that otherwise have syntactic meaning. The right solution depends on where the text will be placed; a representation suitable for ordinary HTML text is not a universal defence for scripts, URLs or arbitrary attributes.\n\nThe [HTML Entity Converter](/html-entity-converter) encodes five common characters and can decode entity references using the browser. It is useful for inspecting examples and preparing text, but it is not an HTML sanitizer or a complete security policy for untrusted content.\n\n## Follow a literal tag through encoding\n\nSuppose a guide needs to display `<b>Sale</b>` as an example rather than making Sale bold. Encoding turns the angle brackets into entity references. In an appropriate HTML text context, the browser then displays the literal brackets instead of treating them as a tag.\n\nThe encoder replaces ampersands first, then less-than, greater-than, double-quote and single-quote characters. The order matters because the new entity references themselves contain ampersands. The tool does not convert every Unicode character into a named or numeric entity; ordinary letters and many other symbols remain as they were.\n\nTry a simple phrase containing an ampersand, such as “tea & cake”, and compare its encoded and decoded forms. Then try a quoted phrase so you can recognise which characters change. Use a harmless sample rather than a real user payload while learning the boundary.\n\n## Avoid encoding the same layer twice\n\nIf you encode text that already contains an entity, the ampersand in that entity is encoded again. The resulting HTML can display the entity notation literally instead of the intended symbol. This is a common reason a page shows an ampersand followed by letters where a single character was expected.\n\nRecord whether a value is raw text or already escaped. Do not solve the problem by repeatedly decoding until the output looks normal: decoding can turn an innocuous-looking representation back into markup characters. The receiving renderer should be responsible for escaping at the correct output boundary.\n\n## Use the current interface\n\n1. Paste a short text sample into the input.\n2. Select the encode action to represent the five supported syntax characters.\n3. Review the output as text before copying it.\n4. Use the decode action when inspecting an existing entity representation.\n5. Test the result in the exact rendering context that will receive it.\n\nThe tool displays its result as text. Copying that result into another application's HTML insertion feature is a different operation with different consequences. A successful conversion does not mean that the copied string can safely be assigned to an HTML-rendering API.\n\n## Match escaping to context\n\nFor a React text node, normal string rendering already provides the relevant text handling; manually pre-encoding can produce unwanted literal entities. For an attribute, quoting and attribute-specific rules matter. JavaScript strings, CSS values and URL components require their own handling.\n\nUse [URL Encoder](/url-encoder) for percent-encoded URL components rather than substituting HTML entities. Use [HTML Formatter](/html-formatter) for a limited readability transformation, while recognising that formatting also does not remove dangerous behaviour.\n\nIf you need to accept untrusted rich HTML, use an established sanitisation workflow with an explicit allowed-element and allowed-attribute policy in the application. This small converter does not inspect destinations, strip event handlers or prove that markup is safe.\n\n## Keep the conversion in perspective\n\nEncoding and decoding happen in the browser. They do not need a server-side entity-conversion request. Nonetheless, confidential text remains confidential after conversion, and clipboard history or screen sharing can expose it. Read the [Privacy Policy](/privacy-policy) for the site's wider context.\n\nThe [entity converter](/html-entity-converter) is most useful when you can state the question precisely: “How is this character represented?” or “What does this entity decode to?” It should not be used to answer the much broader question of whether an entire page or input pipeline is secure.\n\n";
