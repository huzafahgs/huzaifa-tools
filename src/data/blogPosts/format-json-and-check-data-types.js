// Repository-authored editorial content; no visitor HTML is accepted.
export default "A compressed API response is difficult to inspect, but adding line breaks is only the beginning of a useful review. You also need to know whether a field is a number or a string, whether the expected object exists, and whether parsing has changed an unusually large identifier.\n\nThe [JSON Formatter](/json-formatter) parses JSON with the browser's JavaScript parser, then serialises it with two-space indentation or without optional whitespace. It is a practical way to inspect a small response or configuration example. It is not a schema validator, arbitrary-precision data editor or repair service for every JavaScript object literal.\n\n## Use strict JSON as the starting point\n\nJSON property names and string values require double quotes. Comments, trailing commas, undefined and executable expressions do not belong in strict JSON. A JavaScript object copied from source code can therefore fail even though it is valid in its original programming context.\n\nPaste the original into the input and select Format. If an error appears, inspect the location described by the browser's message. Check the preceding delimiter too: a missing comma can make the next otherwise valid field look responsible. Change one issue at a time and preserve the original response when debugging an upstream system.\n\nMinify parses the same way before producing compact output. It is not a fallback that accepts malformed JSON. Both actions require syntactically valid input, and both operate on the parsed representation rather than preserving every source character.\n\n## Work through a small order record\n\nTry this fictional object:\n\n```json\n{\"order\":\"0017\",\"items\":[{\"sku\":\"PEN\",\"quantity\":2}],\"paid\":false,\"note\":null}\n```\n\nAfter formatting, the array and nested item become easier to distinguish. The order identifier remains a string with leading zeros. Quantity is a number, paid is a boolean, and note is explicitly null. Those differences matter to a receiving application: the string \"false\" is not the same JSON value as false.\n\nNext inspect the structure your application expects. Is items an array even when there is only one product? Is note allowed to be null, or should it be omitted? Formatting cannot answer those contract questions. Compare the result with the API's documented schema or a known-good example.\n\nUse Minify when a compact representation is required, but retain the readable version for review. The smaller character count is not a measurement of actual network savings after compression, and it says nothing about the correctness of the order.\n\n## The preservation checks that matter\n\n### Duplicate property names\n\nIf an object repeats the same property name, parsing does not preserve two independent entries in the resulting object. A later value can replace an earlier one. Do not use the formatted output as evidence that the original contained no duplicates. Investigate duplicates in the source when they could explain a missing value.\n\n### Long numeric identifiers\n\nJavaScript numbers have finite precision. An identifier with many digits can change when parsed as a number, before the formatter displays it. Represent identifiers as strings when the data contract allows that; do not convert a numeric field to a string arbitrarily if a receiving system requires a number. MDN documents this [JSON.parse precision issue](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/parse).\n\n### Meaningful strings\n\nWhitespace inside a quoted string is data. A line break represented by an escape sequence is different from inserting an unescaped physical newline into that string. Check addresses, messages and embedded markup as values rather than treating all whitespace as removable decoration.\n\n## A useful review sequence\n\n1. Remove secrets from a debugging sample while preserving the problematic structure.\n2. Paste strict JSON and choose Format.\n3. Check the root type, required keys and nested array boundaries.\n4. Compare number, string, boolean and null values with the receiving contract.\n5. Copy the reviewed output and test it with the actual consumer.\n\nFor a flat object array destined for a spreadsheet, the [JSON to CSV guide](/blog/json-to-csv-flat-data) explains columns, nulls and formula-like strings. [JSON to XML](/json-to-xml) is a different structural conversion and requires its own checks; indentation alone does not make formats interchangeable.\n\n## Local inspection has boundaries\n\nThe formatter's parse and serialise operations run in the browser. They do not execute submitted functions or contact an API named inside a string. That does not make a payload harmless to every later application: a downstream consumer can interpret strings differently.\n\nDo not paste production credentials merely because the operation is local. Shared devices, extensions and clipboard history remain relevant. Keep large production datasets in an approved development environment, and see the [Privacy Policy](/privacy-policy) for site information.\n\nUse the [formatter](/json-formatter) for readability, then use application tests and schema validation for the questions that readability cannot settle.\n\n";
