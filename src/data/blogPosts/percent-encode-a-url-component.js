// Repository-authored editorial content; no visitor HTML is accepted.
export default "A search value such as “tea & cake” cannot simply be pasted between query delimiters and expected to remain one value. The ampersand normally separates parameters. Percent encoding lets the value travel through that syntax without being confused with the syntax itself.\n\nThe [URL Encoder/Decoder](/url-encoder) uses encodeURIComponent and decodeURIComponent. Despite the broad tool name, its most useful boundary is an individual component: a query value, for example, rather than the complete address with its structural punctuation.\n\n## Encode the value, then assemble the address\n\nEnter “tea & cake” and select Encode URL. The result is “tea%20%26%20cake”. A space becomes %20 and the ampersand becomes %26. That result can occupy a query value while a separate ampersand still separates another parameter.\n\nFor example, a hypothetical application can use a path and query shaped like “/search?q=tea%20%26%20cake&page=2”. Here the first ampersand is encoded because it belongs to the search phrase; the second is literal because it separates q from page.\n\nEncoding the complete address as one component also encodes its colon, slashes and query punctuation. That can be appropriate when the complete URL is itself the value of a redirect parameter, but it is not the same as preparing a normal clickable address. Establish which layer you are working on before converting.\n\n## Decode one known layer\n\nPaste “tea%20%26%20cake” and choose Decode URL to recover the original phrase. The decoder requires valid percent escapes and a valid encoded character sequence. A stray percent sign can cause an error instead of being quietly preserved.\n\nThe string “tea+cake” remains “tea+cake” with this decoding function. Some form-encoding conventions use a plus sign to represent a space, but decodeURIComponent does not perform that substitution itself. If your input comes from a submitted form, inspect it using the parser appropriate to that form encoding.\n\nRepeated decoding is another source of confusion. An encoded percent sign is %25. A value containing %2520 becomes %20 after one decode, and a space after another. Those are separate layers. Do not keep decoding until the result looks friendly without knowing whether the application expects a literal escape sequence.\n\n## A safe inspection routine\n\n1. Identify the source: a plain phrase, an existing query value, or a complete URL embedded inside another parameter.\n2. Copy only the component you intend to transform.\n3. Run the appropriate encode or decode action.\n4. Compare a small special-character example with the expected result.\n5. Assemble the surrounding URL using the application's URL-building API where possible.\n6. Inspect the finished address with [URL Parser](/url-parser) before using it.\n\nThe parser can show whether a supposed value accidentally became another query parameter. That structural check is more useful than judging an address by appearance alone. URL encoding preserves a value through syntax; it does not verify that the value is authorised or that a destination is trustworthy.\n\n## Slugs and escaped components solve different problems\n\n[URL Slug Generator](/url-slug-generator) creates a simplified readable path label from text, discarding some characters. Percent encoding is intended to represent characters rather than invent a simplified title. Use a slug when designing a route label, and component encoding when preserving a value inside a URL.\n\nFor multilingual values, the encoding expands characters into percent-escaped UTF-8 bytes. A long encoded representation does not mean the original phrase contained that many visible characters. The destination may still impose its own URL or request-length constraints.\n\n## Privacy and security of address values\n\nThe transform runs in your browser and does not need to visit the supplied address. An encoded secret is still a secret, however. Query strings can later appear in logs, browser history or copied links. Encoding is not a way to make a private token suitable for a public URL.\n\nUse a harmless example to diagnose delimiter behaviour. Review the [Privacy Policy](/privacy-policy) for site-level details and follow the receiving application's handling rules. When the component boundary is clear, the [encoder](/url-encoder) is a convenient way to inspect the exact representation you intend to send.\n\n";
