// Repository-authored editorial content; no visitor HTML is accepted.
export default "A long one-line query is difficult to review because the selected columns, source tables and filter conditions run together. Line breaks can help a reviewer ask better questions. They do not establish that the query returns the intended rows or is safe to execute.\n\nThe [SQL Formatter](/sql-formatter) uppercases a fixed list of keywords and inserts line breaks using regular expressions. It is a lightweight text transformation, not a dialect-aware SQL parser. It neither connects to a database nor executes the submitted query.\n\n## Use a query with visible clause boundaries\n\nTry “select name, score from results where score > 80 order by score”. The formatter separates common clauses and column commas. Read the result by asking what is selected, where it comes from and which rows survive the filter.\n\nThe example is deliberately small. It has no quoted string containing a keyword, no nested query and no dialect-specific syntax. A successful transformation of that sample should not be used as evidence that a complex report query will be preserved.\n\nKeep the original query alongside the formatted copy. A readable review copy is useful; an untested replacement in a production job is a different decision.\n\n## Strings and comments can look like syntax\n\nThe replacement routine does not tokenise SQL. A word such as “from” inside a quoted value can be matched and changed as if it were a clause keyword. Commas inside strings can also receive line breaks. Those changes can alter literal data rather than merely its presentation.\n\nThe routine first collapses whitespace. A line comment whose meaning depends on a newline therefore deserves particular caution. Do not use this utility to transform a production query containing comments or complex quoted values without an exact comparison and dialect-aware validation.\n\nKeyword matching also does not understand every join form, nested structure or vendor extension. The list of recognised words is limited, and indentation is not a complete parse tree.\n\n## Review the logic in a separate pass\n\n1. Save the original SQL.\n2. Format a small, nonsensitive query for inspection.\n3. Compare quoted values, comments and operators character by character where relevant.\n4. Review joins and filters against the intended data question.\n5. Use the database's own tooling to validate syntax and inspect a safe execution plan.\n6. Run authorised tests in an appropriate nonproduction environment before replacing a real query.\n\nFormatting cannot determine whether a join duplicates records, whether a filter excludes nulls unexpectedly, or whether an aggregate answers the business question. Those are data and logic checks. It also cannot make an UPDATE or DELETE safe merely by placing it on a clear line.\n\n## Choose a tool appropriate to the next step\n\n[Text Diff Checker](/text-diff-checker) can compare corresponding lines, but a formatting change shifts line positions and can make its simple output noisy. For a repository change, use the project's normal diff and SQL tooling.\n\nIf a query returns JSON that needs inspection, [JSON Formatter](/json-formatter) addresses the returned representation rather than the query itself. Do not confuse a neatly formatted response with proof that the query selected the correct population.\n\n## Keep sensitive database details out of examples\n\nThe SQL text transformation runs in the browser without a database connection or formatting-server upload. It does not need real passwords, private table values or connection strings. Use a representative fictional schema when asking someone to review formatting behaviour.\n\nClipboard and screen-sharing risks remain independent of local processing. The [Privacy Policy](/privacy-policy) describes site context. Open the [SQL Formatter](/sql-formatter) for a simple review copy, and use dialect-aware tools for changes whose meaning must be preserved exactly.\n\n";
