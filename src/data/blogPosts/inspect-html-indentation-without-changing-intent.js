// Repository-authored editorial content; no visitor HTML is accepted.
export default "Readable markup helps you spot a missing closing tag or understand a component's nesting. But HTML is not just a sequence of interchangeable opening and closing tags: it includes void elements, raw-text sections and whitespace that can affect rendered text.\n\nThe [HTML Formatter](/html-formatter) inserts line breaks between adjacent tags and estimates indentation with regular expressions. It does not parse the complete HTML grammar or validate a document against accessibility and browser rules. Use it as a limited inspection aid for simple fragments.\n\n## Start with a plain nested fragment\n\nTry `<section><h2>Notes</h2><p>Read the source.</p></section>`. Splitting adjacent tags into lines makes the section and its children easier to identify. Compare the content and tag names rather than assuming indentation itself establishes a correct hierarchy.\n\nKeep the original fragment. The routine trims lines and replaces whitespace between adjacent tags, so it is not a byte-preserving view. If the source is a template, a signed fragment or a preformatted example, the exact original may matter.\n\n## Understand the cases a heuristic can misread\n\nVoid elements such as an image or line break do not have normal closing tags. A simple indentation rule can count them as if they opened a new nested level. The resulting indentation may therefore be misleading even when a browser would interpret the source correctly.\n\nRaw script, style and preformatted text require special care. Textual line trimming is not a general way to preserve their semantics. Spaces between inline elements can also affect visible words, so do not assume whitespace between tags is always disposable.\n\nThe tool accepts nonempty input without a full syntax-validation pass. A displayed result does not prove that tags are balanced, attributes are valid or the page is accessible.\n\n## A useful manual inspection sequence\n\n1. Paste a small fragment rather than an entire production page.\n2. Run formatting and compare opening and closing tag names.\n3. Review void elements and any sudden indentation drift.\n4. Check text around inline elements for missing or changed spacing.\n5. Test the fragment in an appropriate local page and inspect its rendered structure.\n6. Use your project's parser-aware formatter for final source changes.\n\nIf you find a structural issue, repair it in the authoritative source and rerun the normal build. Do not treat the formatter's indentation as a specification for how the document must be rewritten.\n\n## Separate adjacent tasks\n\nThe [HTML Minifier](/html-minifier) is a different tool with a conservative whitespace policy and syntax feedback. Its goal is smaller source, not readable indentation. [HTML Entity Converter](/html-entity-converter) changes representations of particular text characters; it is not a markup beautifier.\n\nNone of these operations makes untrusted HTML safe to inject into a page. Formatting does not remove event handlers, unsafe destinations or application-specific risks. Use the receiving framework's safe rendering and sanitisation mechanisms for untrusted content.\n\n## Check meaning after appearance\n\nA neatly indented form can still lack labels, a heading can still be at the wrong semantic level, and a link can still have an unclear purpose. Those questions need an actual page review. Use keyboard navigation and the browser's accessibility information when testing an implemented interface.\n\nThe transform itself runs locally in the browser and does not publish the fragment or execute it as a preview. Copying the result into a live application is a separate action. Avoid exposing private embedded values and consult the [Privacy Policy](/privacy-policy) for site context.\n\nOpen the [HTML Formatter](/html-formatter) for a quick look at a small fragment, retain the source, and use syntax-aware project tooling when correctness depends on the full language.\n\n";
