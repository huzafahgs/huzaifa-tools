// Repository-authored editorial content; no visitor HTML is accepted.
export default "A digest comparison is reproducible only when both sides hash the same bytes. Visually identical text can contain different line endings or Unicode sequences, and a copied snippet may omit the newline present in a source file.\n\nThe [SHA-256 Hash tool](/sha256-hash) uses Web Crypto to compute a genuine SHA-256 digest over UTF-8 text. It returns 64 lowercase hexadecimal characters. It does not hash an uploaded file or authenticate who supplied the message.\n\n## Check a known text vector\n\nFor abc without a newline, the digest is:\n\n```text\nba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad\n```\n\nCompare the complete value rather than just its beginning and end. Change the input to Abc or add a space to observe that the exact text matters. Empty input is also supported and has its own defined digest.\n\n## Diagnose a mismatch at the input boundary\n\n1. Confirm both systems use SHA-256 rather than another algorithm.\n2. Confirm that the source is text encoded as UTF-8.\n3. Check leading spaces, trailing newlines and line-ending conventions.\n4. Check whether either system normalises Unicode or transforms the text.\n5. Recompute from a controlled sample before investigating the larger workflow.\n\nThe tool limits input to 1,000,000 string characters. Web Crypto requires a supported secure browser context; the page reports an error if that capability is unavailable.\n\n## A plain digest is not a signature\n\nAnyone who can read a message can compute its plain digest. If an attacker can replace both a message and an untrusted expected hash, comparing them does not establish authenticity. Obtain expected values through the trust mechanism appropriate to the application.\n\n[HMAC Generator](/hmac-generator) computes a keyed message-authentication value for a different use case. [MD5 Hash](/md5-hash) is relevant to legacy compatibility, with significant security limitations. Do not choose among these solely by the length of the displayed hexadecimal string.\n\n## Text and file bytes are different inputs\n\nCopying visible content from a file into a text box is not necessarily equivalent to hashing the original file. Encoding, metadata, line endings and binary bytes can differ. Use a file-hashing utility when an official download checksum refers to the complete file.\n\nSHA-256 output is not encryption or a general anonymisation method. Predictable secrets can be guessed and hashed for comparison, and a plain fast digest is not a password-storage design.\n\n## Keep inspection local and bounded\n\nThe digest operation runs in the browser without submitting the input to a hash server. Shared devices and copied output still require care. Read the [Privacy Policy](/privacy-policy) for site context.\n\nUse the [SHA-256 tool](/sha256-hash) when you need a reproducible text digest and can state the encoding and whitespace assumptions. Treat any claim of authenticity as a separate trust question.\n\n";
