// Repository-authored editorial content; no visitor HTML is accepted.
export default "A long address can hide the piece you need to understand. A brand name in the path is not the hostname, a hash fragment is not a query parameter, and an encoded ampersand can belong to a value instead of separating fields. Parsing helps make those boundaries visible.\n\nThe [URL Parser](/url-parser) uses the browser's URL constructor and shows protocol, host, hostname, port, path, query, hash and decoded query entries. It does not visit the address, follow redirects or declare that a destination is safe.\n\n## Inspect a concrete address\n\nPaste “https://example.com:8443/search?q=tea%20%26%20cake&page=2#results”. The hostname is example.com, while the host also includes the explicit port. The path is /search. The query contains q and page, and the hash is #results.\n\nThe parameter display shows the decoded q value, making the encoded ampersand understandable as part of “tea & cake”. The raw query remains useful for seeing the original syntax. Reading both views helps explain why a manually assembled URL was interpreted differently from what its author intended.\n\nThis address is a structural example, not a recommendation to visit a service. The parser needs a valid absolute URL; a relative path alone has no base address in this interface.\n\n## Distinguish host from surrounding text\n\nLook at Hostname when identifying the parsed domain. A familiar name elsewhere in a URL can be part of a path, query value or user-information section rather than the actual host. Parsing can expose structure, but it cannot establish the reputation, ownership or safety of that host.\n\nThe browser may normalise some details while parsing. A default port can appear as Default, and internationalised hostnames may have an encoded representation. Do not use this as an exact original-byte preservation tool.\n\n## Query entries can repeat\n\nAn address may contain the same parameter name more than once. The tool iterates the query entries and displays them, rather than collapsing the list into a single object property. The receiving application may choose the first, last or all values depending on its own framework.\n\nThat is a useful debugging question: if a filter appears to be ignored, inspect duplicate names before changing the backend. The parser shows what is present; it does not know which interpretation your application applies.\n\nThe fragment is shown separately. It can identify a location or client-side state, but it is not interchangeable with a normal query parameter. Test the actual application's navigation behaviour when a fragment has a special meaning there.\n\n## Use parsing as a diagnostic step\n\n1. Copy the address without opening it.\n2. Paste the absolute value and run Parse.\n3. Inspect protocol and hostname first.\n4. Compare raw query syntax with decoded parameter entries.\n5. Check duplicate names, unexpected ports and the fragment.\n6. Use the application's URL-building API to correct the source of a malformed address.\n\n[URL Encoder](/url-encoder) can help inspect a single component's percent encoding. If the address will be embedded in a QR code, verify the final destination before using [QR Code Generator](/qr-code-generator); that generator has an external-service processing model, unlike this local parser.\n\n## Private values remain visible\n\nThe parse operation happens locally and does not fetch the destination. However, query strings can contain authentication tokens, customer identifiers or private search terms. Do not paste them into a shared screenshot, and do not assume that an encoded value is anonymised.\n\nThe [Privacy Policy](/privacy-policy) covers the wider site. Open the [URL Parser](/url-parser) when you need structural clarity, then use appropriate network or application checks for redirects, availability and trust.\n\n";
