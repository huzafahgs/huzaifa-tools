// Repository-authored editorial content; no visitor HTML is accepted.
export default "Binary notation expresses an integer using powers of two. A computer's fixed-width storage format can add another layer, including padding and signed encoding. A plain numeric converter does not necessarily implement those storage conventions.\n\nThe [Decimal to Binary tool](/decimal-to-binary) validates a whole decimal integer, checks that it is safely representable and calls the standard base-two conversion. Negative values retain a leading minus sign. There is no bit-width or two's-complement selector.\n\n## Verify thirteen by place value\n\nThirteen is eight plus four plus one, so its binary representation is 1101. The positions from right to left represent one, two, four and eight. The zero in the two's position means that contribution is absent.\n\nThe tool does not pad this to 00001101 automatically. Both strings can represent the same nonnegative integer, but a protocol requiring exactly eight bits needs that width handled explicitly in the appropriate workflow.\n\n## Use the supported integer format\n\n1. Enter a whole decimal number, optionally with a leading minus sign.\n2. Convert and read the binary result.\n3. Sum the powers of two for a small example to check it.\n4. Use [Binary to Decimal](/binary-to-decimal) for a supported reverse check.\n5. Apply any required width or storage convention separately.\n\nDecimal fractions and scientific-notation text are not accepted by the integer syntax check. Do not remove a decimal point to obtain a result: that changes the input rather than converting the same value.\n\n## Negative notation is not a memory layout\n\nConverting negative thirteen gives a minus sign followed by the binary magnitude. It does not produce an eight-bit or sixteen-bit two's-complement pattern. To interpret signed bytes, establish the width and encoding before choosing a conversion method.\n\n[Hex to Decimal](/hex-to-decimal) handles another integer base but also does not infer the meaning of a machine word from its appearance. Representation and application semantics should remain separate.\n\n## Safe range and source handling\n\nThe tool rejects numbers beyond the standard safe-integer range. Use BigInt or another exact-integer implementation for larger values. A rejection is preferable to quietly publishing rounded bits as an exact representation.\n\nThe conversion is local to the browser and does not save a worksheet. Keep the original decimal value and any required bit convention with the copied result. See the [Privacy Policy](/privacy-policy) for site context.\n\nUse [Decimal to Binary](/decimal-to-binary) for plain integer notation, then handle fixed-width protocol requirements in tooling that explicitly supports them.\n\n";
