// Repository-authored editorial content; no visitor HTML is accepted.
export default "Two systems can both use HMAC-SHA-256 and still produce different values because they sign different bytes. Key encoding, whitespace, message order and output representation all belong to the agreement between sender and verifier.\n\nThe [HMAC Generator](/hmac-generator) supports SHA-256, SHA-384 and SHA-512 through Web Crypto. It treats both message and secret as UTF-8 text and emits a hexadecimal authentication value. It does not decode a hex-looking key into binary bytes or implement a particular API's canonical request format.\n\n## Establish the byte agreement first\n\nA key entered as 6162 is the UTF-8 text containing those four digits, not automatically the two bytes representing ab. A protocol that supplies a Base64-encoded or hexadecimal binary key needs a different preparation step and an interface that supports it.\n\nLikewise, a message containing a final newline differs from one without it. The tool requires non-whitespace message and key values but preserves their exact text for encoding once they pass that check.\n\n## Use a reproducible nonsecret sample\n\n1. Choose a harmless sample message and test key.\n2. Select the algorithm required by the protocol.\n3. Generate the value and record the input encoding.\n4. Compare with an independent implementation using the same bytes.\n5. Inspect output representation before comparing with a Base64-encoded reference.\n\nChanging one message character or the key should change the resulting value. That demonstration explains the dependency; it does not prove that a complete authentication protocol is correctly implemented.\n\n## Protocol details remain outside the form\n\nSome APIs sign a combination of method, path, timestamp, headers and body. Others require a sorted or canonicalised representation. This tool signs only the message you supply; it does not assemble those components, apply a freshness policy or verify an incoming request.\n\n[JWT Decoder](/jwt-decoder) only decodes a supported token structure and does not verify its signature. [SHA-256 Hash](/sha256-hash) produces an unkeyed digest. Keep those operations distinct from a shared-secret authentication construction.\n\n## Protect the key throughout the workflow\n\nHMAC is not message encryption. The message can remain readable while its authentication value is checked by someone with the appropriate key. Key distribution, storage and rotation are application responsibilities beyond this generator.\n\nThe operation occurs locally through Web Crypto and does not upload the key for generation. Do not use a real production secret in a shared demonstration or copy it into a support ticket. A compromised device or clipboard remains a separate concern.\n\nConsult the [Privacy Policy](/privacy-policy) for site context. Use the [HMAC Generator](/hmac-generator) for a controlled compatibility check only when the text-key and text-message model matches the intended protocol.\n\n";
