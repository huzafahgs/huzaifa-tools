// Repository-authored editorial content; no visitor HTML is accepted.
export default "A spreadsheet export looks like a table, while an API may expect an array of objects. The conversion is straightforward only after deciding what the headers mean and what data types each column should have. A number-shaped cell can be an identifier, and a comma inside a customer name should not create another column.\n\nThe [CSV to JSON tool](/csv-to-json) reads a header row and turns subsequent nonempty lines into objects. It supports quoted comma values within a single line. It is intended for simple comma-separated records, not every CSV dialect or a multiline spreadsheet export of arbitrary complexity.\n\n## Prepare a small representative sample\n\nStart with a header and two fictional rows:\n\n```csv\nname,code,quantity\n\"Amina, Jr.\",0017,2\nBilal,0021,0\n```\n\nThe first row defines three property names. The quoted comma in the name remains inside one value. The resulting code values are strings, preserving their leading zeros; quantity is also a string, including \"0\". The converter does not infer that a numeric-looking column should become a JSON number.\n\nThat behaviour is useful for product codes and identifiers. If an API requires quantity to be numeric, apply an explicit type conversion in an appropriate data-processing step and validate it there. Do not globally remove quotes from the generated JSON.\n\n## Convert and inspect before copying\n\n1. Paste comma-separated text with a header row and at least one data row.\n2. Ensure every header is nonempty and unique.\n3. Choose Convert to JSON.\n4. Check the reported row count against the intended nonempty records.\n5. Inspect a row with punctuation, a missing value and a leading-zero identifier.\n6. Copy JSON only after confirming the receiving application's expected types.\n\nBlank lines are ignored. Cell values are trimmed, so meaningful surrounding spaces are not preserved. If you are converting fixed-format identifiers where spaces matter, this utility is not a byte-preserving import workflow.\n\n## Headers are keys, not just labels\n\nDuplicate headers are especially risky. If two columns share the same name, a later assignment can replace the earlier value in the object. Rename ambiguous columns at the source rather than accepting a result that silently contains fewer independent fields than your table.\n\nMissing cells become empty strings. Extra cells beyond the header count are not mapped to new named properties. These rules mean that a syntactically successful conversion is not proof that every row had the correct number of columns. Count fields in suspicious records before trusting the result.\n\nAn empty cell, the text \"null\" and a JSON null value are three different things. This converter produces strings; it does not apply a business definition of missing data. Agree on that definition with the consumer before performing a production import.\n\n## Where quoted CSV becomes too complex\n\nThe parser handles a quoted comma and doubled quote characters within a line. It first divides the source into lines, however, so it cannot preserve a quoted field that contains an actual newline. A notes column exported from a spreadsheet can contain exactly that structure.\n\nFor multiline records, use a full CSV parser configured for the actual delimiter and quoting rules. The same applies to semicolon-delimited exports, tab-separated files and large automated pipelines. Do not replace every newline or semicolon blindly: those characters might be part of real values.\n\nKeep the original export intact until the import has been verified. A copy-and-paste utility should not become the only surviving representation of a customer list or inventory file.\n\n## Match the destination's contract\n\nUse the [JSON Formatter](/json-formatter) to review nesting and types after conversion. If you need the opposite direction, the [JSON to CSV guide](/blog/json-to-csv-flat-data) explains its own flat-object rules and spreadsheet formula precautions. The two utilities do not promise a lossless round trip because they apply different structural and value rules.\n\nFor a developer fixture, make the sample intentionally small and nonsensitive. For a business import, validate required fields, duplicates and row totals in the destination before applying changes to live records.\n\n## Local processing does not remove personal information\n\nThe conversion itself happens in the browser and does not upload the pasted table to a conversion server. Names, emails and account numbers still remain personal data in the output. Shared clipboards, screenshots and downloaded copies need the same care as the original export.\n\nRefer to the [Privacy Policy](/privacy-policy) for site-level information. When your data fits the simple single-line model, return to [CSV to JSON](/csv-to-json), verify a representative sample and preserve the source alongside the reviewed result.\n\n";
