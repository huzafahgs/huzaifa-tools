// Repository-authored editorial content; no visitor HTML is accepted.
export default "A random password is useful only if it is accepted by the intended service and stored where you can retrieve it safely. Generation, storage and account authentication are separate steps. A page that produces random characters does not complete the other two.\n\nThe [Password Generator](/password-generator) uses the browser's crypto.getRandomValues and rejection sampling to choose characters from the selected pool. Its length control ranges from 4 to 64, with uppercase, lowercase, number and symbol options. The lower bound is an interface capability, not a recommendation to use a short password.\n\n## Choose options from the receiving service's rules\n\nSelect a suitable length and the allowed character pools before generating. If the service requires a particular character class, inspect the result: selecting a pool makes its characters eligible but does not guarantee that every selected class appears in each generated value.\n\nIf no pool is selected, the tool asks you to choose one. It does not fall back to a hidden default. The symbol pool is a defined set of common punctuation, not every Unicode symbol.\n\n## Complete the workflow deliberately\n\n1. Confirm the destination's password requirements.\n2. Choose length and allowed pools.\n3. Generate a fresh value and inspect compatibility.\n4. Store it in an appropriate password manager.\n5. Complete the account change and verify access through the service's normal process.\n\nDo not reuse a password printed in a tutorial or screenshot. A worked example should demonstrate the controls rather than supply a credential for real use. NIST's [password guidance](https://www.nist.gov/cybersecurity-and-privacy/how-do-i-create-good-password) recommends password managers and emphasises length; it does not endorse this particular website.\n\n## What randomness does and does not establish\n\nThe implementation rejects excess byte values before selecting a character, avoiding a simple modulo bias within the chosen pool. That is a property of the generation routine, not a guarantee that the device, browser extensions or account are uncompromised.\n\nThe [Password Strength Checker](/password-strength-checker) uses a six-rule heuristic and cannot prove the generated value has never been exposed. [UUID Generator](/uuid-generator) uses another mechanism intended for ordinary identifiers and should not be substituted for password generation.\n\n## Handle the result as a secret\n\nGeneration occurs in the browser without a password-generation upload. Copying places the result on the clipboard, where device settings or clipboard managers can matter. Avoid shared screenshots, public notes and unnecessary messages containing the value.\n\nThe page does not provide an encrypted vault, account recovery or a saved password history. If secure generation is unavailable in the browser, the tool reports that failure rather than supplying a deliberately weaker fallback.\n\nReview the [Privacy Policy](/privacy-policy) for site context. Use the [generator](/password-generator) as one step in a complete credential workflow, with unique credentials and the service's supported account protections.\n\n";
