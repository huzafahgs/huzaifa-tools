// Repository-authored editorial content; no visitor HTML is accepted.
export default "A regular expression that matches one example is not yet ready for use. The important test is whether it rejects the near-miss, finds the intended boundaries and behaves consistently across the actual inputs. A useful test set includes the cases that should fail, not just a line that makes the result turn green.\n\nThe [Regex Tester](/regex-tester) constructs a JavaScript RegExp from a pattern and selected flags. It displays nonempty matches and highlights their positions in the test text. It does not validate compatibility with another regex engine or place a time limit around expensive matching.\n\n## Start with a narrow matching question\n\nSuppose you need to find labels shaped like AB-123 in a sample log. A pattern such as “[A-Z]{2}-[0-9]{3}” describes two uppercase ASCII letters, a hyphen and three digits. Test it against AB-123, ab-123, ABC-123 and AB-1234.\n\nThe unanchored pattern can match a substring inside a longer value. AB-1234 contains AB-123 at the beginning. If the requirement is to validate the entire field, add appropriate start and end anchors and test again. If the requirement is to extract labels from prose, decide what surrounding characters are allowed instead of using whole-input anchors blindly.\n\nThis distinction separates searching from validation. A visible match is not evidence that every character in the input meets your rule.\n\n## Use the flags deliberately\n\nThe interface exposes g, i, m and s. Case-insensitive matching changes whether uppercase and lowercase variants can match. Multiline mode changes anchor behaviour around line boundaries. Dot-all mode changes whether a dot can match a newline.\n\nThe current implementation collects results globally even when the g option is cleared: it adds g internally for matchAll. Do not rely on that checkbox to demonstrate first-match-only behaviour. Empty matches are filtered out of the displayed list, so a zero-width pattern can have behaviour that is not represented by a visible result.\n\nIf you are investigating lookarounds, boundaries or an expression that can match an empty string, confirm the exact result in your application runtime as well. The displayed count is a count of the nonempty matches this interface retains.\n\n## Build a compact regression sample\n\n1. Write one example that should match and one that should not.\n2. Add a longer value containing the same substring.\n3. Add case differences and line breaks if the real input can contain them.\n4. Select the intended flags and inspect highlighted boundaries.\n5. Save the pattern, flags and expected outcomes together in your project's tests.\n\nKeep the pattern field separate from JavaScript source-literal syntax. Enter the expression itself rather than surrounding it with slash delimiters and appending flags as text. A slash can be a literal character in a constructor pattern, so adding delimiters can silently change what you are searching for.\n\nFor source code that embeds a regex, the [JavaScript formatting guide](/blog/format-javascript-without-executing) helps with readability. It cannot establish that the regex fulfils a validation requirement. [Text Diff Checker](/text-diff-checker) can compare test fixtures when their line positions remain aligned.\n\n## Avoid patterns that monopolise the browser\n\nMatching occurs as inputs change and runs in the page's JavaScript context. A pathological expression combined with a long near-matching string can be expensive. There is no worker timeout or guaranteed cancellation in this tester.\n\nUse short, nonsensitive samples, especially for patterns with nested repetition or ambiguous alternatives. Do not paste an untrusted expression against a huge log to see what happens. Test performance in a controlled development environment before applying a pattern to unbounded input.\n\nThe operation does not upload the test text to a matching server. Logs can nevertheless contain tokens, email addresses or private query strings. Redact those while preserving the relevant character shape, and review the [Privacy Policy](/privacy-policy) for site context.\n\nOpen the [Regex Tester](/regex-tester) with a precise question and a deliberately mixed test set. The useful result is an understood boundary and saved expectation, not simply a large number of matches.\n\n";
