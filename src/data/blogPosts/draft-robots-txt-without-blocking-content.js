// Repository-authored editorial content; no visitor HTML is accepted.
export default "A robots.txt file communicates crawl rules to cooperating crawlers. A mistaken path can prevent useful pages or resources from being fetched, while a private page can remain accessible to visitors despite appearing in a Disallow rule. The first step is to define the crawl behaviour you want, not to copy a generic block of restrictions.\n\nThe [Robots.txt Generator](/robots-txt-generator) assembles a user-agent line, allow and disallow paths, and an optional sitemap line. It can copy the result or download a text file. It does not install the file, inspect existing rules or test how a crawler resolves a conflict.\n\n## Begin with the site's actual paths\n\nThe interface starts with example paths such as /admin and /private. These are defaults to review, not evidence that your website has those directories or that they should be blocked. Remove or replace them based on the actual site.\n\nEnter one path per line in the allow or disallow field. The generator trims blank surrounding whitespace and emits a directive for each nonempty line. It does not crawl the path, verify that it exists or distinguish a document path from an accidentally pasted complete URL.\n\nKeep the scope narrow and intentional. Blocking the root path has a much broader meaning than blocking a specific section. Do not deploy a draft until you can explain every line and its intended effect.\n\n## Crawl control is not authentication\n\nGoogle's [robots.txt introduction](https://developers.google.com/search/docs/crawling-indexing/robots/intro) distinguishes crawl management from keeping information private. A robots file is public and does not require other clients to obey it. Use real access controls for private content, and use the appropriate indexing mechanism when the objective is exclusion from search rather than crawl scheduling.\n\nDo not list sensitive document names as a substitute for protecting them. The generated file can itself disclose the path names you entered. Also avoid assuming that a blocked page's content-level indexing directive will be seen if the crawler cannot fetch that page.\n\nThese distinctions are why a download button is not a safe deployment decision by itself. The generator formats your intent; it does not establish that the intent is correct.\n\n## Create a small reviewable draft\n\n1. Identify the crawler group you intend to address; an asterisk represents a general group.\n2. Remove the example paths that do not apply.\n3. Enter the exact allowed and disallowed paths you have reviewed.\n4. Add the absolute sitemap URL only when that sitemap exists and should be discoverable.\n5. Generate and inspect the complete text.\n6. Compare it with the current production file before replacing anything.\n\nIf the site already has several agent groups, do not flatten them into one group merely because this interface produces one set. Preserve the existing policy in the actual website workflow. The tool has no merge function for complex existing files.\n\n## Verify the deployed resource\n\nThe downloaded file is named robots.txt, but downloading does not place it at the site's root. Installation depends on the framework or host. After an authorised deployment, request the live root resource and confirm that it returns the intended plain text rather than an application page, redirect loop or error.\n\nCheck important article and tool routes against the intended policy, along with resources needed to render them. Verify the sitemap URL independently. Use [URL Parser](/url-parser) to inspect a suspicious address, remembering that parsing does not fetch it.\n\nThe [Meta Tag Generator](/meta-tag-generator) addresses a different part of page metadata. It does not override a robots file or prove that a page will be indexed. Coordinate changes in the site's existing SEO architecture.\n\n## Conservative maintenance pays off\n\nKeep a versioned copy of the previous working file and record the reason for each change. A small, understood policy is easier to review than a long list copied from unrelated sites. Do not add directives simply because they look technical or appear in a competitor's file.\n\nGeneration takes place in the browser. There is no automated production write or crawler test. The [Privacy Policy](/privacy-policy) covers the site context; the [generator](/robots-txt-generator) supplies a draft for a deliberate review and deployment process.\n\n";
