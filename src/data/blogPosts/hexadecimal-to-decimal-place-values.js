// Repository-authored editorial content; no visitor HTML is accepted.
export default "Hexadecimal is base sixteen, using digits zero through nine and letters A through F. A letter in this context is a digit value, not a variable. Understanding place value makes a conversion easier to verify than treating a hex string as an opaque code.\n\nThe [Hex to Decimal tool](/hex-to-decimal) accepts a signed hexadecimal integer with an optional 0x prefix. It validates the digit syntax and rejects values outside JavaScript's safe-integer range. It does not interpret floating-point encodings, byte order or a complete memory dump.\n\n## Work through 2A\n\nThe rightmost A represents ten. The 2 to its left represents two groups of sixteen. Thus 2A is 2 × 16 + 10 = 42. Enter 2A or 0x2A and compare the decimal result.\n\nLetter case does not change the value, so 2a represents the same integer. A leading minus sign represents a negative mathematical value; it is not an instruction to interpret a fixed-width two's-complement bit pattern.\n\n## Use the syntax check deliberately\n\n1. Copy the intended integer without unrelated separators.\n2. Keep an optional 0x prefix only when it belongs to the value.\n3. Convert and inspect the decimal output.\n4. Verify a small value using powers of sixteen.\n5. Use specialised tooling if the source represents bytes or an encoded floating-point value.\n\nThe tool permits hexadecimal digits rather than arbitrary text. A rejected oversized value should be handled with an exact-integer method, not shortened until it passes; deleting digits changes the number.\n\n## Representation does not determine meaning\n\nFF is 255 as an unsigned mathematical integer. In an eight-bit signed two's-complement context, the same bit pattern can represent another value. This interface does not ask for a width or signed encoding convention, so it does not perform that interpretation automatically.\n\n[Decimal to Binary](/decimal-to-binary) shows another integer representation. [HEX to RGB](/hex-to-rgb) divides a six-digit colour into three channels rather than treating the entire colour value as one decimal quantity.\n\n## Keep exactness within the supported range\n\nThe safe-integer check prevents a successful output that depends on an unrepresentable large integer. This is useful protection, but it also means the tool is not an arbitrary-precision converter.\n\nConversion happens locally without a server request. Avoid exposing private identifiers unnecessarily and read the [Privacy Policy](/privacy-policy) for site context. Use [Hex to Decimal](/hex-to-decimal) when the source is a plain supported integer, with its application meaning documented separately.\n\n";
