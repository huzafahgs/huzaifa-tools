// Repository-authored editorial content; no visitor HTML is accepted.
export default "XML and JSON organise information differently. Converting between them requires choices about elements, attributes, repeated names and text. A generic converter can expose a useful shape, but it cannot know which distinctions your receiving application must preserve.\n\nThe [XML to JSON tool](/xml-to-json) parses XML and recursively constructs a JSON representation. It preserves the root name and groups repeated child names into arrays in common cases. Its current mapping has important limitations, including loss of attributes on text-only leaf elements.\n\n## Trace a simple repeated element\n\nUse `<items><item>A</item><item>B</item></items>`. The root becomes an items property, and the repeated item values become an array containing A and B as strings. This is a useful starting point for understanding how a small list can map into JSON.\n\nNow compare a single item. Its value may appear as a scalar rather than an array because the mapping reacts to repetition. If an API requires an array even for one item, normalise that structure deliberately in a separate processing step. Do not assume one sample proves the shape of all future records.\n\nText values remain strings; a quantity written as 2 does not automatically become a JSON number. Type conversion needs an explicit contract.\n\n## Inspect attributes instead of trusting a general label\n\nThe recursive routine collects attributes under an @attributes property, but a text-only leaf returns its text directly. An element such as `<item code=\"A\">Pen</item>` therefore loses that code attribute in this mapping.\n\nThis limitation is especially relevant to feeds where identifiers, units or language tags live in attributes. A plausible-looking JSON value can be incomplete. Test an attribute-bearing leaf in your sample before deciding that the output is suitable for an import.\n\nParent elements with child elements can retain collected attributes, so the behaviour is not simply “all attributes removed” either. Review the actual output at each level that matters to the application.\n\n## Mixed content is not a lossless transcript\n\nText around nested elements, comments and ordering across different child names can be significant in XML. A property-based object mapping does not provide a universal representation of that sequence. The routine also trims text nodes, which can remove meaningful surrounding whitespace.\n\nFor prose-oriented XML, signed documents or a vocabulary with strict round-trip requirements, use a mapping designed for that vocabulary. Keep the original XML as the authoritative source rather than replacing it with an approximate JSON view.\n\n## A conversion checklist for a real integration\n\n1. Identify which elements, attributes and ordering rules the consumer requires.\n2. Build a sample containing a repeated child, a single child and an attribute-bearing leaf.\n3. Parse and convert the sample in the tool.\n4. Compare every required field with the original XML.\n5. Check scalar-versus-array behaviour and value types.\n6. Use an explicit transformation and schema tests for production imports.\n\n[XML Formatter](/xml-formatter) is useful when you only need to inspect the source hierarchy. [JSON to XML](/json-to-xml) follows another mapping; running the result through it does not automatically restore lost attributes or the original structure.\n\n## Privacy and appropriate scale\n\nConversion runs in the browser and does not upload the XML to a conversion server. The result can still contain the same personal data as the source, and removing one attribute does not anonymise the rest. Use synthetic records for examples and follow the [Privacy Policy](/privacy-policy) for site context.\n\nThis interface is a paste-based utility rather than a streaming data integration system. For a small record, the [converter](/xml-to-json) can help you discover the mapping questions quickly. The finishing step is a field-by-field comparison against the actual consumer's needs.\n\n";
