// Repository-authored editorial content; no visitor HTML is accepted.
export default "A large photograph does not need to retain its camera dimensions when it will appear in a small article card. Resizing changes the number of pixels in the image. It is distinct from lowering an encoder's quality setting and from cropping away part of the composition.\n\n[Image Resizer](/image-resizer) scales the whole source image to your chosen dimensions. It includes aspect-ratio locking and PNG, JPEG or WebP output, followed by a preview and download. Start from a saved original so every comparison uses the same source.\n\n## Choose dimensions for the destination\n\nCheck the image's intended display area before typing a width. A card, a full-width banner and an attachment may require different exports. Where your publishing system uses responsive image candidates, prepare sizes appropriate to that system rather than assuming one file serves every display optimally.\n\nSuppose a source is 2400 by 1600 pixels. With proportions preserved, changing its width to 1200 gives a height of 800. Both dimensions are halved, leaving one-quarter as many pixels. This does not guarantee one-quarter of the file size, because encoding and image detail also matter.\n\nIncreasing dimensions does not reconstruct detail absent from the source. If a small logo is blurry, scaling it up is not a replacement for obtaining a suitable original.\n\n## Load and adjust the image\n\n1. Select a supported source file and confirm its loaded dimensions.\n2. Leave Preserve aspect ratio enabled for a proportional resize.\n3. Change one dimension and inspect the automatically calculated other dimension.\n4. Choose an output format appropriate to the image.\n5. Create the preview, inspect it, and download the result.\n\nThe loader accepts JPEG, PNG, WebP, GIF, BMP or AVIF where the browser can decode the format. Source files are limited to 25 MB and 24 megapixels. Output dimensions must be positive integers, at most 10,000 pixels on either side and within 24 megapixels overall.\n\nThese limits are safeguards, not a promise that every device can comfortably process every file up to the limit. A phone with limited available memory may still need a smaller source.\n\n## Know what the aspect-ratio control does\n\nWhen the lock is enabled, editing one dimension calculates the other from the source proportions, rounded to whole pixels. Turning the lock back on does not automatically repair a previously distorted pair; make a dimension edit and inspect both numbers again.\n\nTurning the lock off permits independent dimensions. That stretches or compresses the entire image if the proportions differ. To fill a differently shaped frame without distortion, use [Image Cropper](/image-cropper) to choose the composition, then resize the cropped image as necessary.\n\n## Choose format and quality separately\n\nPNG provides lossless encoding of the newly produced raster, but resizing has already changed the source pixel grid. JPEG replaces transparent regions with white. WebP is another available output when the browser supports its encoder.\n\nJPEG and WebP expose a quality control. A higher setting is not a target file size or a guarantee of exact visual preservation. If dimensions are already correct and you only want a JPEG compression comparison, use [Image Compressor](/image-compressor). If you only need to change format, use [Image Converter](/image-converter).\n\nThe export does not retain animation or copy original EXIF. Browser decoding may apply orientation and colour conversion, so inspect the preview rather than assuming the exported bytes preserve every property of the source.\n\n## Check the downloaded result\n\nOpen the saved file and confirm its dimensions, orientation, transparency behavior and visible detail at the intended display size. Check file size separately. A lossless PNG export of a photograph can be larger than its JPEG source despite smaller dimensions.\n\nThe processing uses browser image decoding and canvas export without an application upload step. Still inspect private visual details before sharing the output; removing original EXIF is not comprehensive redaction. The [Privacy Policy](/privacy-policy) explains the site's broader context.\n\nKeep the accepted derivative beside the untouched source with a descriptive filename. That makes later corrections possible without repeatedly resizing an already reduced image.\n\n";
