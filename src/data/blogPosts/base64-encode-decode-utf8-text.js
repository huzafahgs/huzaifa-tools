// Repository-authored editorial content; no visitor HTML is accepted.
export default "Base64 often appears in an API example, a configuration value or a debugging log. Its unfamiliar alphabet can make ordinary text look encrypted. In reality, a person who has the value can usually decode it without a secret key. The useful question is what bytes the value represents and which encoding the receiving system expects.\n\nThe [Base64 Converter](/base64-converter) is a text workflow. It encodes JavaScript text as UTF-8 bytes and converts those bytes to standard Base64. In the reverse direction it decodes Base64 bytes as UTF-8 and rejects invalid UTF-8 rather than pretending every binary file is readable text.\n\n## Follow a complete round trip\n\nEnter “Hello” and choose Encode. The result is “SGVsbG8=”. Copy that output into the input and choose Decode to recover “Hello”. The equals sign is padding, not part of the original word.\n\nNow add a space after Hello before encoding. The output changes because that space is part of the original input. The tool rejects an all-whitespace input, but it does not trim meaningful leading or trailing spaces from otherwise nonempty text during encoding.\n\nDecoding removes whitespace around and within the submitted Base64 text before processing. That helps with line-wrapped encodings. It does not remove whitespace from the decoded message itself. If two systems disagree, compare the original text, its newline convention and its character encoding before assuming that one Base64 implementation is broken.\n\n## Why Unicode needs a byte encoding\n\nThe letters in “Hello” are a simple example. Accents, non-Latin scripts and emoji require a defined text encoding before converting to Base64. This tool uses TextEncoder for UTF-8 bytes and a strict UTF-8 TextDecoder for the return path.\n\nMDN's [Encoding API documentation](https://developer.mozilla.org/en-US/docs/Web/API/Encoding_API) describes the distinction between a JavaScript string and encoded bytes. The practical consequence is that a Base64 string containing an image or a differently encoded document may not be decodable as text here, even when the Base64 itself is valid.\n\nDo not “fix” such a result by deleting arbitrary characters. Establish whether the source is actually UTF-8 text. A binary file needs a byte-preserving file decoder, and a data URL also contains a prefix that is not part of the raw Base64 payload.\n\n## Standard Base64 is not every Base64-like format\n\nThe interface uses the browser's standard Base64 functions. Some protocols use a URL-safe alphabet and omit padding. A JWT, for example, has separately encoded parts and a surrounding token structure. Use the [JWT Decoder](/jwt-decoder) to inspect that structure rather than submitting the entire dotted token as one ordinary Base64 value.\n\nIf Base64 must travel inside a URL component, the plus and slash characters require attention to the receiving protocol. [URL Encoder](/url-encoder) performs percent encoding, which is a different operation. Do not repeatedly encode a value until it looks acceptable; document which layer you are encoding and reverse those layers in the correct order.\n\n## A debugging checklist with a clear boundary\n\n1. Confirm that the source represents text rather than arbitrary file bytes.\n2. Confirm that the text encoding is UTF-8.\n3. Identify standard Base64 versus a protocol-specific URL-safe form.\n4. Try a harmless known value such as Hello before using the real sample.\n5. Check spaces and newline characters in the decoded result.\n6. Test the final value in the application that will consume it.\n\nThe tool provides copy actions for the displayed output. It does not save a decoding history or prove that a recovered string is authentic. A successful decode only establishes that the supplied representation can be interpreted under these rules.\n\n## Keep credentials out of demonstrations\n\nBase64 is not password storage, encryption or an access-control mechanism. Encoding a token before posting it in a support ticket does not make it safe to share. Use an invented sample or redact the sensitive value before demonstrating a problem.\n\nThis conversion is carried out in the browser, without a conversion-server upload. Clipboard managers, extensions and shared screens are still relevant. The [Privacy Policy](/privacy-policy) covers the wider site, while your organisation's rules should govern handling of real secrets.\n\nOpen the [Base64 Converter](/base64-converter) when you know you need a UTF-8 text round trip. If your goal is secrecy, authentication or binary-file recovery, choose the workflow designed for that purpose.\n\n";
