// Repository-authored editorial content; no visitor HTML is accepted.
export default "A JWT can look opaque while carrying a readable payload. Decoding it is useful when debugging a test login or checking which claim names an application sends. It does not establish who created the token or whether the application should accept it.\n\nThe [JWT Decoder](/jwt-decoder) expects three dot-separated parts. It decodes the header and payload as JSON and displays the signature part as supplied. The current tool explicitly does not verify that signature. Treat every displayed claim as untrusted input until a proper verifier has performed the required checks.\n\n## Separate the three parts conceptually\n\nThe header contains metadata about the token representation. The payload contains claims. The signature part is relevant to verification, but merely seeing it does not prove that it is correct. The decoder can parse a fabricated three-part value if its first two components contain decodable JSON.\n\nA header field naming an algorithm is not permission to trust that algorithm. The receiving application must apply its own configured verification policy and keys. Likewise, a payload that says a user is an administrator is just a statement in supplied data until the application's authentication and authorisation processes establish otherwise.\n\n## Inspect a test token systematically\n\n1. Use a development token or a deliberately nonsensitive sample that you are authorised to inspect.\n2. Paste it into the decoder and run the decode action.\n3. Read the header and payload separately rather than looking only for a familiar username.\n4. Identify issuer, audience and time-related claims where present.\n5. Compare those values with the consuming application's documented expectations.\n6. Perform verification in the actual application's trusted token library and environment.\n\nThe tool accepts the three-part shape, not every possible token format. A five-part encrypted representation is outside this interface's expected structure. A decode error can mean malformed Base64url or JSON; it does not by itself explain the state of the user's session.\n\n## Use timestamps as evidence with context\n\nClaims such as exp, iat and nbf commonly represent numeric time values in seconds. Check the specification and application contract before interpreting a custom field with a similar name. Confusing milliseconds and seconds can shift a displayed date dramatically.\n\nUse [Unix Timestamp Converter](/unix-timestamp-converter) for a supported seconds value and compare UTC with the local display. A token whose expiration appears to be in the future can still be invalid because of its issuer, audience, signature, revocation handling or application policy. Time is one part of the decision.\n\nThe decoder does not calculate a trusted session status, contact an issuer, retrieve keys or determine whether a token has been revoked. Its displayed JSON is an inspection aid, not an authentication verdict.\n\n## What to include in a debugging report\n\nDescribe the failing operation, expected issuer and audience, and the relevant claim names. Use redacted or invented values where possible. Do not paste a live bearer token into a public issue merely because the payload was easy to decode.\n\nA bearer token may grant access to whoever possesses it. The encoded form and the original token deserve the same handling. Copying decoded JSON can also expose personal data even when the signature is omitted. Share only the minimum authorised information needed to explain the issue.\n\nThe [Base64 text guide](/blog/base64-encode-decode-utf8-text) explains why encoding is different from secrecy. JWT uses its own surrounding format; decoding the whole dotted string as ordinary Base64 is not the right workflow.\n\n## Local processing and downstream trust\n\nThe decode operation runs in the browser without a token-decoding upload. That reduces the need for a separate processing service but does not protect against an unsafe device, extension or shared clipboard. Follow your organisation's rules for real credentials and see the [Privacy Policy](/privacy-policy) for site context.\n\nThe result can help identify a missing claim or a timezone misunderstanding. It cannot make an untrusted claim authoritative. Open the [JWT Decoder](/jwt-decoder) for inspection, and leave acceptance decisions to the verified application flow.\n\n";
