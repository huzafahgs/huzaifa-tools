export default `# Mobile Image Optimization: Faster Loads on Every Device

Mobile visitors often have slower connections and smaller screens, so optimizing images for mobile is essential.

## Resize for small screens

Provide smaller image variants for mobile devices. This keeps pages fast and reduces mobile data use.

## Use efficient formats

AVIF and WebP are especially useful on mobile because they lower file size while preserving visual quality.

## Lazy load non-critical images

Delay loading images that appear below the fold until the user scrolls, improving initial page performance.

## Inspect the requested source on a narrow screen

A narrow layout does not guarantee a small download. Reload with a mobile viewport and inspect the actual image request in the network panel. Compare it with the displayed size and device pixel density. Use responsive source candidates when your site supports them, and verify that the crop keeps faces, labels, or product details visible. Test a slow connection as well as Wi-Fi. Avoid solving mobile speed by making essential diagrams too small to read.

## Continue the workflow

For a complete export-and-check process, see the [practical compression guide](/blog/image-compression-guide-2026).
`;
